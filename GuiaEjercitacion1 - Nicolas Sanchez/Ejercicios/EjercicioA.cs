namespace Ejercicios;

public class EjercicioA
{
  // ============================================
  // SECCIÓN A — Pregunta 1
  // Respuesta: a) private solo es accesible dentro de la clase; protected también es accesible en las subclases.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 2
  // Respuesta: c) La subclase también debe declararse como abstract, o el compilador da error
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 3
  // Respuesta: c) Solo una clase base (herencia simple).
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 4
  // Respuesta: a) virtual tiene implementación y permite override; abstract no tiene cuerpo y lo exige.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 5
  // Respuesta: c) Ilimitadas — una clase puede firmar tantos contratos como necesite.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 6
  // Respuesta: b) Verifica si un objeto es de cierto tipo o implementa una interfaz en tiempo de ejecución.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 7
  // Respuesta: d) Error de compilación — las clases abstractas no pueden instanciarse directamente.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 8
  // Respuesta: b) El de Alumno, porque el objeto real en memoria es un Alumno.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 9
  // Respuesta: b) Solo la misma clase puede acceder a edad directamente.
  // ============================================

  // ============================================
  // SECCIÓN A — Pregunta 10
  // Respuesta: c) Llama al constructor de la clase padre pasando los parámetros, antes de ejecutar el cuerpo del constructor hijo.
  // ============================================
}